# Changelog v1.3.1

## Fixes
- Hardened app update apply step (especially on Windows) to reduce failures caused by file locks.
- Update now stops running app processes before applying changes, retries directory renames with backoff on Windows, and restores previously running processes if the update fails mid-way.
- Improved lock-related error detection so actionable guidance is still shown when retries are exhausted.

