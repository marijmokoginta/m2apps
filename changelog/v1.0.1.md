# M2Apps v1.0.1

## Added

- Interactive root CLI menu with keyboard navigation (`↑`, `↓`, `Enter`, `Esc`).
- Root menu flows for:
  - install application
  - update installed application (with app selection)
  - switch update channel (app + channel selection)
- Cross-platform installer scripts:
  - `install.sh` (Linux/macOS)
  - `install.ps1` (Windows)
- Release notes automation in GitHub Actions using `changelog/<tag>.md`.

## Improved

- Root command now displays explicit app version: `v1.0.1`.
- Release packaging script now includes installer files in distribution structure.

## Notes

- Existing command interfaces (`install`, `update`, `channel`, `daemon`, `list`) remain available.
