# Changelog v1.3.0

## Fixes
- Update engine now resolves versioned release assets dynamically, so app updates keep working even when asset filenames include the release version (for example `app-v1.0.0.zip` -> `app-v1.2.0.zip`).

