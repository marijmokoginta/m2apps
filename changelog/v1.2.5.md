# Changelog v1.2.5

- feat: ensure `APP_URL` synchronization in `.env` immediately after application update
- feat: add `php artisan storage:link` to post-update tasks for Laravel and Laravel-Inertia presets
- refactor: improve application management consistency by adding `SyncAppURL` to process manager
