# M2Apps v1.2.2

## Highlights
- Hotfix release to fix Windows build failure in release workflow.

## Changes
- Fixed variable redeclaration bug in Windows privilege elevation checker (`internal/privilege/elevate_windows.go`).
- Resolved compile error:
  - `no new variables on left side of :=`
- Verified cross-platform build, including explicit Windows target build.

## Validation
- `GOOS=windows GOARCH=amd64 go build ./...`
- `go test ./...`

## Version
- Application version bumped to `v1.2.2`.
