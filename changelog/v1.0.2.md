# M2Apps v1.0.2

## Fixed

- GitHub release workflow now checks out repository source in the `release` job before reading changelog files.
- Resolved failure:
  - `Changelog file not found!`
  - when reading `changelog/<tag>.md` during release creation.

## Changed

- Application version bumped to `v1.0.2`.
